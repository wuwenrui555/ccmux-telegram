"""ccmux_telegram — Telegram bot frontend for the ccmux backend library.

Composition root lives in `ccmux_telegram.main`; everything else is
internal to this package. No public API here on purpose — this is a
leaf application, not a library.
"""
